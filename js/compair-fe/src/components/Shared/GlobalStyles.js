import styled from 'styled-components';
import { CSS_CONST } from './constants';


export const AppContainer = styled.div`
  padding: 0;
  @media all and (max-width: ${CSS_CONST.screenmd}) {
    padding: 0;
  }
 
`;
